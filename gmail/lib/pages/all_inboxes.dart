import 'package:flutter/material.dart';

class AllInboxes extends StatelessWidget {
  const AllInboxes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("All Inboxes"),
      ),
      body: Center(
        child: Text("NO content available"),
      ),
    );
  }
}