import 'package:flutter/material.dart';

class Primary extends StatelessWidget {
  const Primary({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PRimary Mails"),
      ),
      body: Center(
        child: Text("NO content available"),
      ),
    );
  }
}